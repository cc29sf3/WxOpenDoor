<!--pages/admin/admin.wxml-->
<button bindtap="addSecurity">添加</button>
<button bindtap="init">init</button>

<view wx:key="id" wx:for="{{securities}}">
  <navigator url="../editUser/editUser?id={{item.id}}&name={{item.name}}&phone={{item.phone}}">
  {{index+1}}: {{item.name}}:{{item.phone}}
  </navigator>

</view>
