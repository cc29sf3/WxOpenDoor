<!--pages/admin/admin.wxml-->
<i-button bind:click="addSecurity">添加</i-button>
<button bindtap="init">init</button>

<i-cell-group>
    <i-cell wx:for="{{securities}}" title="{{item.name}}" label="{{item.phone}}" value="编辑" is-link url="../editUser/editUser?id={{item.id}}&name={{item.name}}&phone={{item.phone}}" only-tap-footer></i-cell>
</i-cell-group>

